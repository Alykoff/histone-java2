[
  {
    "name": "global -> getTimeStamp, wait",
    "cases": [
      {
        "input": "--{{var a = getTimeStamp()}}{{wait(1000)}}{{var b = getTimeStamp()}}{{b - a}}",
        "expectedResult": "--1"
      },
      {
        "input": "--{{var a = getTimeStamp()}}{{var b = wait(1000, ((x) => x + getTimeStamp()), 1)}}{{b - a}}",
        "expectedResult": "--2"
      },
      {
        "input": "--{{var a = getTimeStamp()}}{{var b = wait(1000, => getTimeStamp())}}{{b - a}}",
        "expectedResult": "--1"
      }
    ]
  }
]