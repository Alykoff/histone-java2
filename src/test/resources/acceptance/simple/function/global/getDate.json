[
  {
    "name": "global -> getDate",
    "cases": [
      {
        "input": "{{global->getDate() != undefined}}",
        "expectedResult": "true"
      },
      {
        "input": "{{global->getDate('+3y-2d')->toJSON}}",
        "expectedResult": ""
      }
    ]
  }
]